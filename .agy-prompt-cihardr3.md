ONE-SHOT MODE — exactly ONE response; no follow-up turn. Do NOT ask questions; review now.
Show your work: "Checks performed", then "Findings" (or none), THEN the final line.
A bare verdict line is a FAILED review.
End with EXACTLY one final line: VERDICT: APPROVE | APPROVE_WITH_NOTES | REQUEST_CHANGES

REVIEW TARGET: /Users/nick/Developer/Mail/unleashed-mail-plugin/.claude/worktrees/opus5-review/docs/planning/CI_WORKFLOW_HARDENING_PLAN.md

This is **ROUND 3** of a mandatory pre-implementation plan review for a **Claude Code plugin**
repository (`UnleashedServices/unleashed-mail-plugin`) — you are reviewing the plugin's own assets as
software, not Swift app code. Repo root: /Users/nick/Developer/Mail/unleashed-mail-plugin/.claude/worktrees/opus5-review
Open the files the plan cites and check them. Run code where behaviour is claimed.

The plan batches `COREDEV-2598` (CI proves the plugin loads), `COREDEV-2600` (drift guard for
duplicated primitives) and `COREDEV-2603` (the verdict artifact's absolute plan path).

## What changed in round 3

Round 2 was gemini APPROVE / codex REQUEST_CHANGES (5 findings). All five re-verified by execution —
four confirmed, one refuted — and §10 records each. The substantive changes:

1. **§4.1(b) no longer relies on version equality.** Round 2's backstop was "installed `version` equals
   the checkout's", which fails whenever `main` and the branch share a version — i.e. most changes. It
   now requires **installed-BYTE identity** (a digest over the payload, or a per-run sentinel token),
   with version equality demoted to a smoke check. **Mutant 3 must now be run with the two versions
   deliberately equal.** Judge whether offering the implementer two identity mechanisms re-introduces
   the "X or Y" defect the plan spent two rounds removing — §8 asks this directly.
2. **§4.1(c) now launches the MCP server from the INSTALLED `.mcp.json` declaration**, not from a known
   path. Round 2's wording let an implementation hard-code the script path, which would keep mutant 2
   green even when `.mcp.json` was repointed. Hooks assertions also move to the installed tree.
3. **§4.3's `..`-guard mutation proof was IMPOSSIBLE and is removed.** `_repo_root` returns an ancestor,
   so `relpath` can never escape; with no root the helper returns absolute before the guard is reached.
   Executed both ways — byte-identical. The guard is relabelled unproved defence-in-depth, and
   assertion 4 is narrowed to "outside every git repository". **Verify this reasoning**; if the guard IS
   observable under some input, that is a high-value finding.
4. **§4.2's last "X or Y" is decided** — `sessionstart-restore.sh` is left unchanged, with reasoning —
   and item 1 gains a mandatory vector that runs with `paths.sh` **absent**, so the three inline
   fallbacks are actually exercised rather than bypassed.
5. **A round-2 finding was REFUTED:** the upstream `marketplace add "$GITHUB_WORKSPACE"` line number.
   It is line 27 (line 25 is the `export`, line 29 is `plugin list`). Verify if you wish.

## Ground rules

- LOCKED maintainer decisions — do not re-litigate: effort floor `xhigh`; three model tiers; direct
  Anthropic API only; CLI pin 2.1.220; autonomous mode user-invoked only.
- Judge the PLAN, not the absence of code. Nothing is implemented yet by design.
- CI is `ubuntu-latest` only; the dev host is macOS.
- **Over-correction is a valid finding.** Round 3 changed a lot; say so if anything true was weakened.

## The highest-value question

Every item now names the mutants it must reject. **Is there a plausible implementation of any item that
passes its entire stated proof set and is still wrong?** Round 2 shipped a mutation that could not fail
by construction, inside the very section that added the "reject plausible wrong implementations" rule —
so this failure mode is demonstrated, not hypothetical.

## Answer directly

§8 records four answered questions and one new one: does §4.1(b) offering **two** byte-identity
mechanisms (digest comparison, or per-run sentinel) count as an unresolved contract, or is it an
acceptable implementer's choice because both are fully specified and independently sufficient?
