ONE-SHOT MODE — exactly ONE response; no follow-up turn. Do NOT ask questions; review now.
Show your work: "Checks performed", then "Findings" (or none), THEN the final line.
A bare verdict line is a FAILED review.
End with EXACTLY one final line: VERDICT: APPROVE | APPROVE_WITH_NOTES | REQUEST_CHANGES

REVIEW TARGET: /Users/nick/Developer/Mail/unleashed-mail-plugin/.claude/worktrees/opus5-review/docs/planning/COREDEV-2597_REDACTOR_DEFECTS_PLAN.md

ROUND 4 of a mandatory pre-implementation plan review for a **Claude Code plugin** repo
(`UnleashedServices/unleashed-mail-plugin`) — the plugin's own assets are the software under review,
not Swift app code. Repo root: /Users/nick/Developer/Mail/unleashed-mail-plugin/.claude/worktrees/opus5-review
The plan is FROZEN at commit `16a73e2` for the duration of this review. Open cited files; run code
where behaviour is claimed.

## What changed in round 4

Round 3 was gemini APPROVE_WITH_NOTES / codex REQUEST_CHANGES (4). All re-verified by execution.

1. **A parity bug in round 3's own prescribed implementation was found and fixed.**
   `pk_abcdefgh-sk-ijklmnop` produced two replacements in shell and ONE in Python, because Python used
   a combined alternation that matches greedily from the leading `pk_`. **Python now uses two
   sequential passes** in the shell's fragment order. Verify the fix and try to break it again — this
   is the single most load-bearing change in the plan.
2. **§4.4's exemption set went from two to FIVE.** Three leak-direction divergences were found:
   `Bearer\n<token>`, `api\nkey: <value>` and `bearer<NBSP><token>` are redacted by Python and MISSED
   by the shell. Verify each. A fourth reported example, `apiKey=abcdefgh`, was **REFUTED** — check
   that too.
3. **§2's rationale contradicted §4.6's own table**, and the claim that every plan filename matches
   `[A-Z0-9_-]+_PLAN.md` was false. `@2x` is now a deferral on SCOPE, not a dismissal on impossibility.
4. **Rule ordering is now pinned** (`~sk-abcdefgh123/`), and mutation accounting corrected from two to
   four mutants plus the parity mutant.

## The one question the plan explicitly refuses to decide alone

§8 asks whether `hook_redact_pii` should fold newlines with `tr` **before** `sed` rather than after.
Doing so closes exemption entries 3 and 4 — two real leaks — while changing **no pattern**. Against it:
every rule would then see text across former line boundaries, which is strictly more matching, and §3's
rule is *never widen*.

**Give a definite recommendation, with reasoning.** This is a genuine design fork and the plan will not
resolve it without a ruling.

## What to assess

- **Factual accuracy above all** — spot-check citations, run the claims.
- **Is there still a plausible implementation that passes every stated proof and is wrong?** Round 3's
  prescribed implementation failed its own parity requirement, so this failure mode is demonstrated,
  not hypothetical.
- **Over-correction** — four rounds of edits; say so if anything true was weakened or lost.
- **Internal consistency** — §2 vs §4.6, §4.4 vs §4.2, §6/§7 mutation counts.

## Ground rules

LOCKED, do not re-litigate: effort floor `xhigh`; three model tiers; direct Anthropic API only; CLI pin
2.1.220; autonomous mode user-invoked only. Judge the PLAN, not the absence of code. macOS/BSD sed
host; CI is `ubuntu-latest` only.
