ONE-SHOT MODE — exactly ONE response; no follow-up turn. Do NOT ask questions; review now.
Show your work: "Checks performed", then "Findings" (or none), THEN the final line.
A bare verdict line is a FAILED review.
End with EXACTLY one final line: VERDICT: APPROVE | APPROVE_WITH_NOTES | REQUEST_CHANGES

REVIEW TARGET: /Users/nick/Developer/Mail/unleashed-mail-plugin/.claude/worktrees/opus5-review/docs/planning/COREDEV-2597_REDACTOR_DEFECTS_PLAN.md

This is **ROUND 2**. You reviewed round 1 of this plan. Read the target fresh — it has been
substantially rewritten.

You are performing a MANDATORY pre-implementation plan review for a **Claude Code plugin** repository
(`UnleashedServices/unleashed-mail-plugin`). This repo ships the agents, skills, hooks, scripts and a
bundled MCP server used to develop a separate macOS email app. **You are reviewing the plugin's own
assets as software** — not Swift app code.

Repo root (a git worktree, branch `claude/plugin-opus5-review-xs81o0`, HEAD `0e110d8`):
/Users/nick/Developer/Mail/unleashed-mail-plugin/.claude/worktrees/opus5-review
You have read access to the whole tree. **Open the files the plan cites and check them.**

## What happened between round 1 and round 2

Every round-1 finding from both reviewers was re-verified **by execution** before the plan was edited.
Several were confirmed, several were corrected, and **three reviewer claims were refuted or narrowed.**
The plan's new §10 records this. **You are explicitly invited to contest any of it** — if a refutation
is wrong, that is a high-value finding.

The refutations you should test hardest, because the plan now depends on them:

1. **"The proposed boundary omits non-ASCII word characters"** — the plan says this is REFUTED, because
   the character immediately before `sk-` in the cited `café-sk-…` example is the **hyphen**, not the
   accented letter, and under true adjacency (`cafésk-…`) both candidate guards behave identically.
   **Test this yourself.** If there is a case where the accent decides the outcome, say so.

2. **"`Icon@2x.png` corruption is a live defect (High)"** — the plan now rates it Medium on the strength
   of a new §4.6 caller audit: of the four shell callers of `hook_redact_pii`, three are structurally
   incapable of carrying such literals and the fourth writes to a log with no reader. **Check the audit
   table line by line.** If any row is wrong, the severity argument collapses.

3. **The `~` rule.** Round 1 asked which of two rules to adopt; the two reviewers picked **opposite**
   ones and their mandated tests were mutually exclusive. Round 2 adopts **neither** — it adopts the
   conjunction (leading boundary + first char `[A-Za-z_]` + a required following `/`) on the grounds
   that each rule alone leaves a measured corruption class. It also adds a `:t`/`tt` sed loop, claiming
   that the leading-boundary guard **both** reviewers assumed is what *creates* a shell/Python parity
   bug on `~a/~b/x`. **Verify the loop claim by execution** — it is the single most load-bearing new
   mechanism in the plan.

## What to assess

- **Factual accuracy above all.** The plan cites `file:line` throughout, and round 2 added many new
  citations. Spot-check them. A wrong line number, a misquoted behaviour, or a claim contradicted by
  the code is the highest-value finding you can return. Run code where behaviour is claimed.
- **Have the new decisions been stated as decisions?** Round 1's central defect was offering choices
  where a contract was needed. Check that §4.1's boundary class and §4.2's `~` rule are now
  unambiguous, and that no "X or Y" survives anywhere.
- **Proof adequacy — the bar has been raised.** §3 now has a *second* corollary: a mutation proof must
  reject **plausible wrong implementations**, not only reverts. Check each proof against that. §4.1
  names two anti-implementation mutants, §4.2 names a loop mutant, §4.4 requires production-side
  mutations. **Are they sufficient? Is there a plausible implementation that passes the whole set and
  is still wrong?** That is the highest-value question in this review.
- **Internal consistency** — §2 vs §4.4 vs §8 previously gave three different answers to one ownership
  question. Verify that is now settled, and that no new contradiction was introduced.
- **Over-correction.** Round 2 changed a lot. Did anything true in round 1 get removed or weakened?
- **Risk register honesty** — is anything material missing or understated?

## Ground rules

- These maintainer decisions are LOCKED. Do not re-litigate them; assess only whether the plan
  implements them correctly: effort floor `xhigh` (cost accepted); three model tiers; direct Anthropic
  API only; long context in scope so `opus[1m]` must be legal; CLI pin 2.1.220; autonomous mode is
  explicitly user-invoked only.
- Judge the PLAN, not the absence of code. Nothing is implemented yet by design.
- The environment is macOS with **BSD sed** and no GNU sed. CI is `ubuntu-latest` only. The plan is
  explicit about which claims were executed on which engine — check that the honesty holds.

## Answer these directly

§8 now marks all three round-1 questions ANSWERED and poses two new ones. Give a recommendation on the
two new ones:

1. §4.1 treats **underscore as a boundary**, so `foo_sk-…` redacts — and `orders_pk_customer_id_idx`
   redacts with it. The alternative (`\W`) preserves the SQL identifier but lets `OPENAI_KEY_sk-proj-…`
   survive. Is leak-prevention the right trade for a repo whose reviewer evidence discusses GRDB schemas?
2. §4.2 leaves `~9lives/x` unredacted (digit-leading username + slash). Is that reachable enough to
   matter, given `useradd --badname` exists?
