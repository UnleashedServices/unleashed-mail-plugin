ONE-SHOT MODE — exactly ONE response; no follow-up turn. Do NOT ask questions; review now.
Show your work: "Checks performed", then "Findings" (or none), THEN the final line.
A bare verdict line is a FAILED review.
End with EXACTLY one final line: VERDICT: APPROVE | APPROVE_WITH_NOTES | REQUEST_CHANGES

REVIEW TARGET: /Users/nick/Developer/Mail/unleashed-mail-plugin/.claude/worktrees/opus5-review/docs/planning/COREDEV-2597_REDACTOR_DEFECTS_PLAN.md

This is **ROUND 3** of a mandatory pre-implementation plan review for a **Claude Code plugin**
repository (`UnleashedServices/unleashed-mail-plugin`) — you are reviewing the plugin's own assets as
software, not Swift app code. Repo root: /Users/nick/Developer/Mail/unleashed-mail-plugin/.claude/worktrees/opus5-review
Open the files the plan cites and check them. Run code where behaviour is claimed.

## What changed in round 3

Round 2 was gemini APPROVE / codex REQUEST_CHANGES (7 findings). All seven were re-verified by
execution — six confirmed, one refuted — and §10 records each. The substantive changes:

1. **§4.1's boundary is now ASYMMETRIC between the two prefixes** — underscore is a boundary before
   `sk-` but NOT before `pk_`. This is the one genuinely novel construct in the plan and the thing most
   worth attacking. It is two `-e` fragments rather than one. **Verify the claimed behaviour by
   execution** (`OPENAI_KEY_sk-proj-…` redacts; `orders_pk_customer_id_idx` and
   `idx_pk_customer_id_lookup` are preserved; `pk_live_…` and `(pk_…)` still redact) and try to find an
   input where the two fragments interact badly, or where shell and Python diverge.
2. **§4.3's shadow test now emits a stderr marker.** Round 2's version could not prove the
   `2>/dev/null` half — a mutant adding `LC_ALL=C` but dropping the redirect passed both assertions.
   Check the new four-case table actually separates the halves.
3. **§4.4 no longer claims a surviving tilde divergence** — round 2 cited a lookahead that §4.2 deletes.
   Confirm the exemption set is now exactly two entries and that no third is implied anywhere.
4. **§4.6's caller audit was materially wrong and is rewritten.** `docs/planning/AppIcon@2x.png_PLAN.md`
   satisfies the `*_PLAN.md` glob and reaches model context. The claim is weakened from "structurally
   impossible" to "an unenforced naming convention". **Check the corrected table row by row.**
5. **Proof sets hardened:** an exactly-8-character threshold positive, an underscore-leading
   `~_daemon/x`, an explicit `~9lives/x` pin, and a true-adjacency `cafésk-…` row. The §4.1 mutant list
   is now four. **Is there still a plausible implementation that passes the whole set and is wrong?**
   That is the highest-value question in this review.
6. **Two claims narrowed:** `schema.py` is precedent for the SLASH only (its rule is `^~[^/]+/`), and
   the non-ASCII refutation holds only between the two ASCII classes — a Unicode-aware `(?<!\w)`
   preserves `cafésk-…`.
7. **A round-2 finding was REFUTED:** BSD `sed` does not reject `-e ':t;s#…#…#'`; it exits 0, warns
   `unused label`, and performs no substitution. Verify that, since the plan now depends on it.

## Ground rules

- LOCKED maintainer decisions — do not re-litigate: effort floor `xhigh`; three model tiers; direct
  Anthropic API only; CLI pin 2.1.220; autonomous mode user-invoked only.
- Judge the PLAN, not the absence of code. Nothing is implemented yet by design.
- macOS host with **BSD sed**, no GNU sed; CI is `ubuntu-latest` only. The plan states which claims were
  executed on which engine — check that honesty holds.
- **Over-correction is a valid finding.** Round 3 changed a lot; say so if anything true was weakened.

## Answer directly

§8 now records four answered questions and one new one. The new one: §4.1's asymmetric rule is two
`sed` fragments in the same invocation as §4.2's tilde loop. **Can you construct an input where
rule ordering changes the result?** Eight adjacency vectors were tested with zero mismatches; find a
ninth or confirm the property.
