ONE-SHOT MODE — exactly ONE response; no follow-up turn. Do NOT ask questions; review now.
Show your work: "Checks performed", then "Findings" (or none), THEN the final line.
A bare verdict line is a FAILED review.
End with EXACTLY one final line: VERDICT: APPROVE | APPROVE_WITH_NOTES | REQUEST_CHANGES

REVIEW TARGET: /Users/nick/Developer/Mail/unleashed-mail-plugin/.claude/worktrees/opus5-review/docs/planning/CI_WORKFLOW_HARDENING_PLAN.md

ROUND 4 of a mandatory pre-implementation plan review for a **Claude Code plugin** repo
(`UnleashedServices/unleashed-mail-plugin`) — the plugin's own assets are the software under review,
not Swift app code. Repo root: /Users/nick/Developer/Mail/unleashed-mail-plugin/.claude/worktrees/opus5-review
Batches `COREDEV-2598` (CI proves the plugin loads), `COREDEV-2600` (drift guard), `COREDEV-2603` (the
verdict artifact's absolute plan path).

**The plan is FROZEN at commit `16a73e2` for the duration of this review.** Round 3 was correctly
refused as a blocker because the target changed mid-review; that will not recur. Verify the target is
unmodified (`git status --porcelain docs/planning/CI_WORKFLOW_HARDENING_PLAN.md` should be empty) and
say so if it is not.

## What changed in round 4

Round 3 was gemini REQUEST_CHANGES (2) / codex REQUEST_CHANGES (5, one a blocker). All re-verified by
execution.

1. **§4.1(b) now mandates ONE identity mechanism** — a per-run sentinel appended to
   `scripts/precompact-snapshot.sh` in the scratch copy, asserted present under `installPath`. The
   digest alternative is explicitly rejected with reasons. Both reviewers said the two-mechanism
   wording was an unresolved contract; judge whether the single mandate is now unambiguous, and whether
   the sentinel carrier is well chosen.
2. **Mutant 4 was circular and is replaced.** "Delete the check → it must still fail, because mutants
   1–3 target its assertions" — delete the check and nothing is left to reject anything. Now a
   one-at-a-time assertion-removal mapping, asserted on the step's **outer** exit status.
3. **The `round` type claim was wrong and is corrected.** `precompact-snapshot.sh:67` serialises
   `"round":"%s"`, so the field is **always** a JSON string; the change is `"09"` → `"9"`, a value
   normalisation, not a numeric `9`. Verify.
4. **§4.2 item 1's fallback proof is now a three-environment matrix**, because the single `env -u HOME`
   case accepted a hard-coded `/.claude/unleashed-mail` as indistinguishable from the correct dynamic
   expansion. Verify the matrix actually separates them.
5. **`errors` must be absent or empty** on the `plugin list --json` entry, alongside `enabled == true`.

## What to assess

- **Factual accuracy above all** — spot-check citations, run the claims.
- **Is there still a plausible implementation of ANY item that passes its entire stated proof set and
  is wrong?** Round 3 shipped a circular mutant and a proof that accepted a hard-coded constant, so
  this failure mode is demonstrated twice over, not hypothetical.
- **Is any residual choice left in §4?** Round 2 claimed none remained and round 3 found two. If you
  find a third, that is the highest-value finding available.
- **Over-correction** — four rounds of edits; say so if anything true was weakened or lost.
- **Batching** — C remains batched on the finding that the verdict artifact is unauthenticated. Still
  right?

## Ground rules

LOCKED, do not re-litigate: effort floor `xhigh`; three model tiers; direct Anthropic API only; CLI pin
2.1.220; autonomous mode user-invoked only. Judge the PLAN, not the absence of code. CI is
`ubuntu-latest` only; the dev host is macOS.
