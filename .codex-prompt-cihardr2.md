ONE-SHOT MODE — exactly ONE response; no follow-up turn. Do NOT ask questions; review now.
Show your work: "Checks performed", then "Findings" (or none), THEN the final line.
A bare verdict line is a FAILED review.
End with EXACTLY one final line: VERDICT: APPROVE | APPROVE_WITH_NOTES | REQUEST_CHANGES

REVIEW TARGET: /Users/nick/Developer/Mail/unleashed-mail-plugin/.claude/worktrees/opus5-review/docs/planning/CI_WORKFLOW_HARDENING_PLAN.md

This is **ROUND 2**. You reviewed round 1 of this plan. Read the target fresh — it has been
substantially rewritten.

You are performing a MANDATORY pre-implementation plan review for a **Claude Code plugin** repository
(`UnleashedServices/unleashed-mail-plugin`). This repo ships the agents, skills, hooks, scripts and a
bundled MCP server used to develop a separate macOS email app. **You are reviewing the plugin's own
assets as software** — not Swift app code.

Repo root (a git worktree, branch `claude/plugin-opus5-review-xs81o0`, HEAD `0e110d8`):
/Users/nick/Developer/Mail/unleashed-mail-plugin/.claude/worktrees/opus5-review
You have read access to the whole tree. **Open the files the plan cites and check them.**

This plan batches three tickets: `COREDEV-2598` (CI proves the plugin loads), `COREDEV-2600` (drift
guard for duplicated primitives), `COREDEV-2603` (the verdict artifact's absolute plan path).

## What happened between round 1 and round 2

Every round-1 finding from both reviewers was re-verified **by execution** before the plan was edited.
All four codex findings held, but two were **materially incomplete**, and one reviewer recommendation
was **refuted**. The plan's new §10 records this. **You are explicitly invited to contest any of it.**

The claims you should test hardest, because the plan now depends on them:

1. **The wrong-checkout install was REPRODUCED.** Running the cited upstream recipe verbatim
   (`claude plugin marketplace add "$GITHUB_WORKSPACE"` + `install`) against this repo's manifest
   git-cloned **main** into the cache and reported `enabled: true` — installing v2.5.3 over the
   branch's 2.6.1. Check `.claude-plugin/marketplace.json` and judge whether §4.1(a)'s scratch-copy
   source rewrite is the right remedy, and whether the stated fidelity gap is acceptable.

2. **`enabled: true` proves neither hooks nor MCP.** The plan claims issue #61's own duplicate-hooks
   defect installs, reports enabled, AND passes `claude plugin validate --strict` — so an MCP handshake
   alone is inert against the very failure §4.1 cites. Hence the extra assertion that `plugin.json` must
   have no `hooks` key. **Is that assertion sufficient, and is it the right mechanism?**

3. **`COREDEV-2603` is NOT a security change.** Round 1 rated relaxing the absolute `planPath` a Medium
   provenance risk, and one reviewer wanted 2603 split out as security-relevant. Round 2 refutes that:
   the verdict artifact is **unauthenticated**, and a hand-written one with fabricated transcript
   digests and non-existent transcript paths produces `GATE OK — APPROVE`, exit 0, on a plan no
   reviewer has read. **Verify this against `scripts/review-verdict.py` yourself.** If it is wrong, the
   whole §4.3 argument — and the decision to keep C batched — collapses.

4. **The doc-fallback was refuted as an alternative**, on the grounds that it fixes strictly *less*
   than repo-relative and is equally useless for CI, so it does not dominate. Round 2 adopts **both**,
   doc-first. Judge that ordering.

5. **§4.2's counts were corrected** — three bash round scanners not two, four `mtime` sites in three
   shapes not two — and the motivating symptom was replaced: `"09"` vs `9` is cosmetic (it reaches only
   a human hint string), while the real defect is a **missing digit cap** that makes the PreCompact hook
   leak `integer expression expected` to stderr, breaking the fail-open invariant. **Verify both the
   corrected counts and the stderr leak.**

## What to assess

- **Factual accuracy above all.** Round 2 added many new citations. Spot-check them. A wrong line
  number or a claim contradicted by the code is the highest-value finding you can return.
- **Is §4.2 now an implementation contract?** Round 1's defect was "single-source **or** compare".
  Verify no "X or Y" survives, and that the per-primitive decisions are individually justified — the
  plan gives a *different* answer for each of the three primitives, which is either precision or
  inconsistency. Decide which.
- **Proof adequacy — the bar has been raised.** §3's corollary now requires each mutation to be one a
  real implementer might write. §4.1 names three mutants, §4.2 gives per-item behavioural criteria,
  §4.3 has five assertions. **Is there a plausible implementation that passes the whole set and is
  still wrong?** That is the highest-value question here.
- **Two named traps** are called out as things that would silently hollow a proof: mutating
  `mcp_server.py` at EOF (it ends in `raise SystemExit(main())`, so appended code is dead), and the
  existing verdict fixture using `tempfile.mkdtemp()` rather than a real git repo. Are there others?
- **Batching.** C is kept in the batch on the strength of claim 3 above. If you disagree with claim 3,
  say what should change.
- **Over-correction.** Round 2 changed a lot. Did anything true in round 1 get removed or weakened?

## Ground rules

- These maintainer decisions are LOCKED: effort floor `xhigh`; three model tiers; direct Anthropic API
  only; CLI pin **2.1.220**; autonomous mode user-invoked only.
- Judge the PLAN, not the absence of code. Nothing is implemented yet by design.
- CI is `ubuntu-latest` only; the dev host is macOS. The plan states which claims were executed where —
  check that the honesty holds.

## Answer this directly

§8 poses one new question: §4.3 Part 2 changes a provenance field on the strength of the finding that
the artifact is unsigned anyway. Should the ordering be **C1 (docs) → C2 (repo-relative) → COREDEV-2497
(authenticity)**, or **C1 → 2497 → C2**? Give a recommendation.
