ONE-SHOT MODE — exactly ONE response; no follow-up turn. Do NOT ask questions; review now.
Show your work: "Checks performed", then "Findings" (or none), THEN the final line.
A bare verdict line is a FAILED review.
End with EXACTLY one final line: VERDICT: APPROVE | APPROVE_WITH_NOTES | REQUEST_CHANGES

REVIEW TARGET: /Users/nick/Developer/Mail/unleashed-mail-plugin/.claude/worktrees/opus5-review/docs/planning/AGENT_OUTPUT_STYLE_PLAN.md

You are performing a MANDATORY pre-implementation plan review for a **Claude Code plugin** repository
(`UnleashedServices/unleashed-mail-plugin`). This repo ships the agents, skills, hooks, scripts and a
bundled MCP server used to develop a separate macOS email app. **You are reviewing the plugin's own
assets as software** — not Swift app code.

Repo root (a git worktree, branch `claude/plugin-opus5-review-xs81o0`): /Users/nick/Developer/Mail/unleashed-mail-plugin/.claude/worktrees/opus5-review
You have read access to the whole tree. **Open the files the plan cites and check them.**

## What this plan is part of

Epic COREDEV-2582 has three sibling plans that must land in order:
1. `docs/planning/OPUS5_ALIGNMENT_PLAN.md` (COREDEV-2583) — lands FIRST
2. `docs/planning/AUTONOMOUS_END_TO_END_PLAN.md` (COREDEV-2584)
3. `docs/planning/DECISION_JOURNAL_PLAN.md` (COREDEV-2585)

## What to assess

- **Factual accuracy above all.** The plan cites `file:line` throughout. Spot-check them. A wrong line
  number, a misquoted behaviour, or a claim contradicted by the code is the highest-value finding you
  can return. Run code where behaviour is claimed.
- **Completeness of the fix.** Does each finding's stated fix actually close the finding? Is there a
  case the fix misses?
- **Proof adequacy.** Each finding claims a proof/test. Would that test actually fail if the fix were
  reverted (mutation-proof discipline)? Name any that would not.
- **Internal consistency** — between sections, and with the sibling plans where they interlock.
- **Risk register honesty** — is anything material missing or understated?
- **Scope discipline** — anything that should be split out or is out of scope.

## Ground rules

- These maintainer decisions are LOCKED. Do not re-litigate them; assess only whether the plan
  implements them correctly: effort floor `xhigh` (cost accepted); three model tiers; direct Anthropic
  API only; long context in scope so `opus[1m]` must be legal; CLI pin 2.1.220; autonomous mode is
  explicitly user-invoked only; the decision journal uses the A+C (append-only journal + rewritten
  live-state) design.
- Do NOT propose `PostCompact` for context injection — it cannot inject context, and the repo has
  already resolved this correctly.
- Judge the PLAN, not the absence of code. Nothing is implemented yet by design.

## ROUND 8 — the mutation-proof design was rebuilt; verify it

`COREDEV-2602` adapts output-style rules from `ayghri/i-have-adhd` (MIT, pinned `07684c4a`) into a new
`AGENT_CONTRACTS.md` §13. Maintainer steer: **adapt to this tool, do not migrate verbatim, do not break
what already works.**

Round 7: both `REQUEST_CHANGES` on the same High finding — **the marker assertions did not work.**
Rule 10's marker collided (appears in the disposition, the precedence clause and a risk row), and a
sweep found the inverse defect in rules 3, 4 and 9, whose markers did not appear literally in their own
rows because markdown emphasis broke the string.

Fixed by making extraction **row-scoped**: §13 → disposition table → the single row for rule N, asserting
the marker within that row. All eight markers were verified programmatically as literal substrings of
their own disposition rows.

Verify:
1. **Is the row-scoped design actually sound?** Would deleting a rule's disposition now fail its
   assertion, and would a *correct* document pass? Try to construct a case that still false-passes or
   false-fails.
2. **Are all eight markers literal and unique-enough within their row?** Re-derive independently.
3. **Rule 7 stays a bare adopt** on tested evidence: prose `Completed:`/`Remaining:` can inject a
   spurious trailer field, but `reviewer-roster.sh:244` emits `REMAINING` only in the `PARTIAL` branch,
   so it is never forwarded on a `COMPLETE`. **Verify that reasoning**, and that §15 records the
   reviewer disagreement fairly.
4. Two Lows: §4.4 now says eight non-bare dispositions; `capture.py` cited at `:327` (documented) and
   `:369` (implemented) rather than `:323`. Verify.
5. No regression: six contracts, ten dispositions (2 adopt / 7 adapt / 1 restate), placement, §7 order,
   and records §8/§10–§15.

**If your findings are cosmetic, say so explicitly and return APPROVE_WITH_NOTES rather than
REQUEST_CHANGES** — this is a documentation plan on its eighth round and the maintainer needs to know
whether anything material remains.

Context: `COREDEV-2583` APPROVED and lands first; `2584`/`2585` PAUSED; `2597`–`2600` own the other
adoptions. Not blockers here.
